# input phase
employee_last_name = input("Enter employee last name: ")
job_level = int(input("Enter job level: "))
salary = float(input("Enter salary: "))
# process phase
if job_level >= 10:
   bonus_rate =0.25
elif 5 <= job_level <= 9:
  bonus_rate = .20
else:
    price_per_ticket = .10
bonus = salary * bonus_rate
# output phase
print ("Employee name: ", employee_last_name)
print ("Bonus amount: $", format(bonus, ",.2f"), sep="")
print ("Job level" , job_level)
